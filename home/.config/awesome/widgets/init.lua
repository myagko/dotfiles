return {
	calendar = require("widgets.calendar"),
	text_input = require("widgets.text_input"),
	hover_button = require("widgets.hover_button"),
	separator = require("widgets.separator"),
	remote_watch = require("widgets.remote_watch")
}
